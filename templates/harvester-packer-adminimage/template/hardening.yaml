# Hardening configuration for the golden image build pipeline.
# NOTE: CIS profile enforcement is not yet consumed by packer/_build (issue #93);
# the security tooling below is installed via the merged packages.yaml.
hardening:
  cisProfile: ${{ values.cisProfile }}
  tooling:
{%- for tool in values.tooling %}
    - ${{ tool }}
{%- endfor %}
